Programming languages differ in when and how they associate values with types. In dynamically typed languages, the concrete type of a value is generally determined while the program runs rather than being fixed by a mandatory compile-time declaration. Optional annotations can still describe intended inputs, outputs, and interfaces for developers and analysis tools. Such annotations communicate expectations without necessarily changing the runtime typing behaviour of the language.

Optimization methods adjust numerical parameters so that a model's objective function becomes smaller or larger as required. In machine learning, an optimizer uses information about the loss landscape to decide how parameters should change after an inaccurate prediction. Gradient-based methods rely on derivatives that indicate how sensitive the loss is to each parameter. The optimizer performs the update itself, while another procedure may be responsible for computing the gradients that the optimizer receives.

Statically typed languages commonly require or infer type information before a program executes, allowing many incompatible operations to be detected during compilation or static analysis. Function signatures can state expected input and output types, and tools can use those contracts to reason about interfaces. Some languages combine static analysis with inference so that programmers do not need to write every type explicitly. This differs from a dynamically typed language in which runtime values determine concrete types during execution.
